os:
    - linux
    - osx
sudo: required
dist: trusty
language: generic
osx_image: xcode10.2
env:
    - SWIFT_VERSION=swift-5.0
install:
    - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
    - swift build
    - swift build -c release
    - swift test